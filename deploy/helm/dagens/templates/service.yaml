apiVersion: v1
kind: Service
metadata:
  name: {{ include "dagens.fullname" . }}
  labels:
    {{- include "dagens.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "dagens.selectorLabels" . | nindent 4 }}
